// ─── Minimal Layout for /gcc/iv-theropy/ar redirect ─────────────────────────
import type { ReactNode } from "react";

export default function OldSpellingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
